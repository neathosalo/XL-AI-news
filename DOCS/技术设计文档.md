# 技术设计文档

## 1. 项目架构

```
ai-news-radar/
├── DOCS/                    # 项目文档
│   ├── 需求规格说明书.md
│   ├── 任务清单.md
│   └── 技术设计文档.md       # 本文档
├── scripts/
│   └── update_news.py       # 后端抓取脚本
├── assets/
│   ├── app.js              # 前端逻辑
│   ├── styles.css          # 样式
│   └── logo.svg
├── data/                   # 抓取的数据
│   ├── latest-24h.json
│   └── ...
└── index.html              # 主页面
```

---

## 2. 星标功能技术设计

### 2.1 前端实现

**文件**：`assets/app.js`

**存储结构**（LocalStorage）：
```javascript
{
  "xl-ai-news-starred": JSON.stringify([
    {
      "id": "新闻唯一ID",
      "title": "新闻标题",
      "url": "新闻链接",
      "starred_at": "2026-02-24T10:00:00Z"
    }
  ])
}
```

**核心函数**：
```javascript
// 收藏新闻
function starItem(item) {
  const starred = getStarredItems();
  if (!starred.find(i => i.id === item.id)) {
    starred.push({...item, starred_at: new Date().toISOString()});
    localStorage.setItem('xl-ai-news-starred', JSON.stringify(starred));
  }
}

// 获取已收藏列表
function getStarredItems() {
  return JSON.parse(localStorage.getItem('xl-ai-news-starred') || '[]');
}

// 切换收藏状态
function toggleStar(item) {
  // 已有则移除，无则添加
}
```

### 2.2 UI设计

- 每条新闻旁边添加 ⭐ 按钮
- 点击切换收藏状态
- 新增"⭐ 已收藏"标签页/筛选按钮

---

## 3. 热门相似主题技术设计

### 3.1 后端实现

**文件**：`scripts/update_news.py`

**新增数据字段**：
```python
def extract_keywords_from_items(items: list[dict]) -> list[dict]:
    """从新闻标题中提取关键词"""
    keyword_counts = {}
    keyword_items = {}

    for item in items:
        title = item.get('title', '')
        # 简单分词（可后续升级为jieba）
        words = simple_tokenize(title)

        for word in words:
            if is_valid_keyword(word):
                keyword_counts[word] = keyword_counts.get(word, 0) + 1
                keyword_items.setdefault(word, []).append(item)

    # 返回出现次数最多的关键词
    hot_topics = [
        {
            "keyword": word,
            "count": count,
            "related_items": keyword_items[word][:5]  # 最多5条
        }
        for word, count in sorted(keyword_counts.items(), key=lambda x: -x[1])[:20]
    ]

    return hot_topics
```

**关键词过滤**：
- 过滤长度 < 2 的词
- 过滤常见停用词（的,了,在,是,我,有...）
- 过滤数字和特殊字符

### 3.2 数据输出

在 `latest-24h.json` 中新增字段：
```json
{
  "generated_at": "2026-02-24T02:16:59Z",
  "total_items": 1255,
  "hot_topics": [
    {
      "keyword": "Claude",
      "count": 15,
      "related_items": [
        {"title": "...", "url": "...", "site_name": "..."},
        {"title": "...", "url": "...", "site_name": "..."}
      ]
    },
    {
      "keyword": "GPT-5",
      "count": 8,
      "related_items": [...]
    }
  ],
  "items": [...]
}
```

### 3.3 前端展示

**新增区域**：
```html
<section id="hot-topics">
  <h2>🔥 热门话题</h2>
  <div class="topic-list">
    <!-- 关键词标签 -->
    <span class="topic-tag">Claude (15)</span>
    <span class="topic-tag">GPT-5 (8)</span>
    <!-- 点击展示相关新闻 -->
  </div>
</section>
```

**交互**：
- 点击关键词标签展开相关新闻列表
- 支持关键词搜索过滤

---

## 4. 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `scripts/update_news.py` | 添加关键词提取函数，输出 hot_topics |
| `assets/app.js` | 添加星标功能逻辑 |
| `assets/styles.css` | 添加星标按钮、热门话题样式 |
| `index.html` | 添加热门话题展示区域 |

---

## 5. 测试计划

### 星标功能测试
- [ ] 点击星标按钮收藏新闻
- [ ] 刷新页面后星标仍在
- [ ] 取消收藏功能正常

### 热门话题测试
- [ ] 热门话题正确显示
- [ ] 点击关键词显示相关新闻
- [ ] 关键词按热度排序

---

**更新日期**：2026-02-24
